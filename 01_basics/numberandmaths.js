// Num and Maths bhot interesting subject ha javascript me isky through ap Ecommerce website and banking website ya applications per working krengy
// there are following example below

const number = 1000
// console.log(number);

// console.log(number.toFixed(2)) // this is used for decimal point after dot

const otherNumber = 10000000000
// console.log(otherNumber.toLocaleString("en-IN")); // this is used for breakup the value that we can easily understand the format the value. Converts a number to a string by using the current or specified locale.

const otherNumber1 = 124.157892
// console.log(otherNumber1.toPrecision(3)) // Returns a string containing a number represented either in exponential or fixed-point notation with a specified number of digits

// =========================MATHS=============================

// console.log(Math + " Math Just and Object" )
// console.log(Math.abs(-8) + " Negative Values convert to Positive");
// console.log(Math.round(4.4) + " Round Values ");
// console.log(Math.ceil(4.4) + " Show Top Value");
// console.log(Math.floor(4.4) + " Show Down Value");
// console.log(Math.min(6, 2, 8, 9, 20, 32, 0 ))
// console.log(Math.max(6, 2, 8, 9, 20, 32, 0))
console.log(Math.random()) // " Values Show Between 0 to 1"

const min = 10 
const max = 30

console.log(Math.random() + 1);

console.log(Math.round(Math.random() + max) - min );

